# Yandex SmartCaptcha PHP SDK

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![PHP Version](https://img.shields.io/badge/PHP-8.0%2B-blue)](https://www.php.net/)

PHP SDK для [Yandex SmartCaptcha](https://yandex.cloud/ru/services/smartcaptcha) с полной поддержкой Laravel.

[English version](README.md)

## Содержание

- [Возможности](#возможности)
- [Требования](#требования)
- [Установка](#установка)
- [Начало работы с Yandex SmartCaptcha](#начало-работы-с-yandex-smartcaptcha)
  - [Предварительные требования](#предварительные-требования)
  - [Создание аккаунта Yandex Cloud](#1-создание-аккаунта-yandex-cloud)
  - [Создание капчи в консоли](#2-создание-капчи-в-консоли)
  - [Получение ключей капчи](#3-получение-ключей-капчи)
  - [Получение IAM-токена](#4-получение-iam-токена-для-управления-через-api)
- [Быстрый старт](#быстрый-старт)
- [Методы API](#методы-api)
- [Интеграция на фронтенде](#интеграция-на-фронтенде)
- [Правило валидации Laravel](#правило-валидации-laravel)
- [Обработка ошибок](#обработка-ошибок)
- [Логирование](#логирование)
- [Тестирование](#тестирование)
- [Участие в разработке](#участие-в-разработке)
- [История изменений](#история-изменений)
- [Поддержка](#поддержка)

## Возможности

- ✅ **Полное покрытие API**: Все методы SmartCaptcha API
- 🔐 **Валидация токенов**: Проверка токенов пользователей
- 📦 **Управление капчами**: Создание, обновление, удаление и список капч
- 🔑 **Получение секретных ключей**: Доступ к серверным ключам
- 🎨 **Интеграция с Laravel**: Service provider, facade и конфигурация
- 📝 **Типобезопасность**: Полная типизация PHP 8.0+ и strict types
- 🪵 **PSR-3 логирование**: Опциональная поддержка логирования
- 🧪 **Хорошо протестировано**: Комплексное покрытие тестами
- 📖 **Хорошо документировано**: Подробная документация и примеры

## Требования

- PHP 8.0 или выше
- Guzzle HTTP client 7.0+
- Laravel 8.0+ (опционально, для интеграции с Laravel)

## Установка

Установка через Composer:

```bash
composer require tigusigalpa/yandex-smartcaptcha-php
```

## Начало работы с Yandex SmartCaptcha

### Предварительные требования

Перед началом работы необходимо настроить Yandex Cloud и создать капчу:

#### 1. Создание аккаунта Yandex Cloud

1. Перейдите в [консоль управления Yandex Cloud](https://console.yandex.cloud)
2. Войдите в Yandex Cloud или зарегистрируйтесь, если вы еще не зарегистрированы
3. На странице [Yandex Cloud Billing](https://center.yandex.cloud/billing/accounts) убедитесь, что у вас подключен платежный аккаунт и он находится в статусе `ACTIVE` или `TRIAL_ACTIVE`
4. Если платежного аккаунта нет, [создайте его](https://yandex.cloud/ru/docs/billing/quickstart/)

#### 2. Создание капчи в консоли

1. В [консоли управления](https://console.yandex.cloud) выберите каталог
2. Выберите сервис **Yandex SmartCaptcha**
3. Нажмите кнопку **Создать капчу**
4. Введите имя капчи. Требования к имени:
   - Длина: от 2 до 63 символов
   - Может содержать строчные буквы латинского алфавита, цифры и дефисы
   - Первый символ — буква, последний — не дефис
5. (Опционально) Отключите проверку имени домена при необходимости
6. Укажите список сайтов, на которых будет размещаться капча (например, `example.com`)
7. Внешний вид оставьте стандартным
8. Настройте капчу по умолчанию:
   - Выберите **тип основного задания** (чекбокс или слайдер)
   - Выберите **тип дополнительного задания** (задание с картинками и текстом)
   - Выберите **сложность**: Легкая, Средняя или Сложная
9. (Опционально) Включите или отключите использование информации об HTTP-запросах для улучшения ML-моделей
10. Нажмите кнопку **Создать**

#### 3. Получение ключей капчи

После создания капчи:

1. В [консоли управления](https://console.yandex.cloud) выберите каталог
2. Выберите сервис **Yandex SmartCaptcha**
3. Нажмите на имя капчи
4. На вкладке **Обзор** скопируйте:
   - **Ключ клиента** (для фронтенд-виджета)
   - **Ключ сервера** (для валидации на бэкенде)

#### 4. Получение OAuth-токена (для управления через API)

Для управления капчами через API необходим OAuth-токен. Пакет автоматически обменяет его на IAM-токен и обновит при необходимости.

**Получение OAuth-токена:**

Перейдите по следующей ссылке и авторизуйте приложение:

```
https://oauth.yandex.ru/authorize?response_type=token&client_id=1a6990aa636648e9b2ef855fa7bec2fb
```

После авторизации вы получите OAuth-токен. Скопируйте его и используйте в вашем приложении.

**Примечание:** Пакет использует `tigusigalpa/yandex-cloud-client-php`, который обеспечивает:
- Автоматический обмен OAuth на IAM-токен
- Кэширование IAM-токена (действителен 12 часов)
- Автоматическое обновление токена при истечении

Теперь у вас есть:
- ✅ Ключ клиента (для фронтенда)
- ✅ Секретный ключ сервера (для валидации)
- ✅ OAuth-токен (для управления через API - автоматически обменивается на IAM)
- ✅ ID каталога (из консоли)

## Быстрый старт

### Базовое использование (чистый PHP)

```php
use Tigusigalpa\YandexSmartCaptcha\SmartCaptchaClient;

// Создаем клиент с OAuth-токеном
// Пакет автоматически обменяет его на IAM-токен и обновит при необходимости
$client = new SmartCaptchaClient($oauthToken);

// Валидируем токен пользователя
$result = $client->validate(
    token: $_POST['smart-token'],
    secret: 'ваш-серверный-секретный-ключ',
    ip: $_SERVER['REMOTE_ADDR']
);

if ($result->isValid()) {
    echo "✅ Человек подтвержден!";
} else {
    echo "❌ Обнаружен бот!";
}
```

### Использование в Laravel

#### 1. Публикация конфигурации

```bash
php artisan vendor:publish --tag=smartcaptcha-config
```

#### 2. Настройка окружения

Добавьте в `.env`:

```env
YANDEX_SMARTCAPTCHA_OAUTH_TOKEN=ваш-oauth-токен
YANDEX_SMARTCAPTCHA_SECRET_KEY=ваш-секретный-ключ
YANDEX_SMARTCAPTCHA_CLIENT_KEY=ваш-клиентский-ключ
YANDEX_SMARTCAPTCHA_FOLDER_ID=ваш-folder-id
```

**Примечание:** Пакет использует `yandex-cloud-client-php` для аутентификации. Ваш OAuth-токен будет автоматически обменян на IAM-токен и обновлен при необходимости (IAM-токены истекают через 12 часов).

#### 3. Использование фасада

```php
use Tigusigalpa\YandexSmartCaptcha\Laravel\Facades\SmartCaptcha;

// Валидация токена
$result = SmartCaptcha::validate(
    request()->input('smart-token'),
    config('smartcaptcha.secret_key'),
    request()->ip()
);

if ($result->isValid()) {
    // Пользователь подтвержден
}
```

## Методы API

### Валидация токена

Проверка токена капчи пользователя:

```php
$result = $client->validate(
    token: 'токен-пользователя',
    secret: 'серверный-секретный-ключ',
    ip: '192.168.1.1' // опционально, но рекомендуется
);

// Проверка результата
if ($result->isValid()) {
    echo "Статус: {$result->status}";
    echo "Хост: {$result->host}";
}
```

### Создание капчи

Создание новой капчи:

```php
$captcha = $client->createCaptcha(
    folderId: 'b1g0ijbfaqsn12345678',
    name: 'моя-капча',
    options: [
        'allowedSites' => ['example.com'],
        'complexity' => 'MEDIUM', // EASY, MEDIUM, HARD
        'preCheckType' => 'CHECKBOX', // CHECKBOX, SLIDER
        'challengeType' => 'IMAGE_TEXT',
    ]
);

echo "ID капчи: {$captcha->id}";
echo "Клиентский ключ: {$captcha->clientKey}";
```

### Получение информации о капче

Получение данных капчи:

```php
$captcha = $client->getCaptcha('id-капчи');

echo "Название: {$captcha->name}";
echo "Сложность: {$captcha->complexity}";
echo "Создана: {$captcha->createdAt}";
```

### Список капч

Получение списка всех капч в каталоге:

```php
$result = $client->listCaptchas(
    folderId: 'b1g0ijbfaqsn12345678',
    pageSize: 50
);

foreach ($result['captchas'] as $captcha) {
    echo "- {$captcha->name} ({$captcha->id})\n";
}

// Пагинация
if ($result['nextPageToken']) {
    $nextPage = $client->listCaptchas(
        folderId: 'b1g0ijbfaqsn12345678',
        pageSize: 50,
        pageToken: $result['nextPageToken']
    );
}
```

### Обновление капчи

Обновление настроек капчи:

```php
$captcha = $client->updateCaptcha(
    captchaId: 'id-капчи',
    updates: [
        'name' => 'новое-название',
        'complexity' => 'HARD',
        'allowedSites' => ['example.com', 'test.com'],
    ]
);
```

### Удаление капчи

Удаление капчи:

```php
$operation = $client->deleteCaptcha('id-капчи');
echo "ID операции: {$operation['id']}";
```

### Получение секретного ключа

Получение серверного секретного ключа:

```php
$secretKey = $client->getSecretKey('id-капчи');
echo "Секретный ключ: {$secretKey->serverKey}";
```

## Интеграция на фронтенде

### Базовый виджет

Добавьте в HTML:

```html
<form method="POST">
    <div id="captcha-container"></div>
    <button type="submit">Отправить</button>
</form>

<script src="https://smartcaptcha.yandexcloud.net/captcha.js" defer></script>
<script>
    window.smartCaptcha = {
        sitekey: 'ваш-клиентский-ключ',
        callback: function(token) {
            console.log('Капча пройдена!');
        }
    };
</script>
```

### Расширенный виджет

```html
<div id="captcha-container"></div>

<script src="https://smartcaptcha.yandexcloud.net/captcha.js?render=onload&onload=onloadFunction" defer></script>
<script>
    function onloadFunction() {
        if (window.smartCaptcha) {
            const container = document.getElementById('captcha-container');
            
            const widgetId = window.smartCaptcha.render(container, {
                sitekey: 'ваш-клиентский-ключ',
                hl: 'ru',
                callback: function(token) {
                    console.log('Токен:', token);
                }
            });
        }
    }
</script>
```

## Правило валидации Laravel

Создайте кастомное правило валидации:

```php
namespace App\Rules;

use Illuminate\Contracts\Validation\Rule;
use Tigusigalpa\YandexSmartCaptcha\Laravel\Facades\SmartCaptcha;

class SmartCaptchaRule implements Rule
{
    public function passes($attribute, $value): bool
    {
        $result = SmartCaptcha::validate(
            $value,
            config('smartcaptcha.secret_key'),
            request()->ip()
        );
        
        return $result->isValid();
    }
    
    public function message(): string
    {
        return 'Пожалуйста, пройдите проверку капчи.';
    }
}
```

Использование в контроллере:

```php
$request->validate([
    'smart-token' => ['required', new SmartCaptchaRule()],
    'email' => 'required|email',
]);
```

## Обработка ошибок

Все исключения наследуются от `SmartCaptchaException`:

```php
use Tigusigalpa\YandexSmartCaptcha\Exceptions\{
    SmartCaptchaException,
    AuthenticationException,
    ValidationException,
    NotFoundException,
    RateLimitException
};

try {
    $result = $client->validate($token, $secret);
} catch (AuthenticationException $e) {
    // Неверный IAM токен или секретный ключ
} catch (ValidationException $e) {
    // Неверные параметры запроса
} catch (NotFoundException $e) {
    // Капча не найдена
} catch (RateLimitException $e) {
    // Слишком много запросов
} catch (SmartCaptchaException $e) {
    // Другие ошибки
}
```

## Логирование

Включение логирования в Laravel:

```php
// config/smartcaptcha.php
'logging' => [
    'enabled' => true,
    'channel' => 'stack',
],
```

Или передайте PSR-3 логгер в чистом PHP:

```php
use Monolog\Logger;
use Monolog\Handler\StreamHandler;

$logger = new Logger('smartcaptcha');
$logger->pushHandler(new StreamHandler('path/to/smartcaptcha.log'));

$client = new SmartCaptchaClient($iamToken, null, $logger);
```

## Тестирование

Запуск тестов:

```bash
composer test
```

С покрытием:

```bash
composer test-coverage
```

## Документация

- [Документация Yandex SmartCaptcha](https://yandex.cloud/ru/docs/smartcaptcha/)
- [Справочник API](https://yandex.cloud/ru/docs/smartcaptcha/api-ref/)
- [Руководство по началу работы](https://yandex.cloud/ru/docs/smartcaptcha/quickstart)

## Лицензия

Лицензия MIT. Подробности в файле [LICENSE](LICENSE).

## Автор

**Игорь Сазонов**
- GitHub: [@tigusigalpa](https://github.com/tigusigalpa)
- Email: sovletig@gmail.com

## Участие в разработке

Мы приветствуем ваш вклад! Не стесняйтесь отправлять Pull Request.

### Настройка окружения для разработки

1. Клонируйте репозиторий:
```bash
git clone https://github.com/tigusigalpa/yandex-smartcaptcha-php.git
cd yandex-smartcaptcha-php
```

2. Установите зависимости:
```bash
composer install
```

3. Запустите тесты:
```bash
composer test
```

### Стандарты кодирования

Проект следует стандартам PSR-12:

```bash
composer cs-check  # Проверка стандартов
composer cs-fix    # Исправление стандартов
composer phpstan   # Статический анализ
```

### Процесс создания Pull Request

1. Сделайте форк репозитория
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Добавлена новая функция'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## История изменений

### [1.0.0] - 2024-11-29

**Добавлено:**
- Первый релиз
- Клиент SmartCaptcha с полной поддержкой API
- Валидация токенов
- Управление капчами (создание, чтение, обновление, удаление, список)
- Получение секретных ключей
- Интеграция с Laravel (service provider, facade, config)
- Поддержка PSR-3 логирования
- Комплексная документация (EN/RU)
- DTO классы для типобезопасности
- Обработка исключений
- Поддержка PHP 8.0+ со strict types

## Поддержка

- [GitHub Issues](https://github.com/tigusigalpa/yandex-smartcaptcha-php/issues)
- [Поддержка Yandex Cloud](https://yandex.cloud/ru/support)
